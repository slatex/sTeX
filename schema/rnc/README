This directory contains the sTeX schema for intermediate files 

odmoc+ltxml.rnc     the schema integration
omdoc		    	the omdoc schema
LaTeXML			the LaTeXML schema
LaTeXML.rnc       	a local copy of LaTeXML/LaTeXML.rnc but without URN referencing
schemas.xml		reference this in the emacs nxml schema locating files. 
README			this file

the rest of the files are experimental, and are not used in production (yet). 
