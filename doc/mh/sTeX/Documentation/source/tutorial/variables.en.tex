\documentclass[lang={en,de}]{stex}
\libinput{docpreamble}
\setsectionlevel{section}
\begin{document}
  \begin{sfragment}{Variables}

    \usemodule{concepts?Variable}

    There is one more symbol that occurs in the formulae that we have not
    yet marked up: The $x$. Unlike the concepts considered
    so far, $x$ is a \emph{\sn{variable}} rather than a
    generally existent constant. More precisely, it is
    a \sn{variable} that is bound and quantified over
    by the sentence fragment ``for all'' -- but we will ignore that for
    now.

    \inputref{concepts/Variable.en}

    In our case, the \sn{variable} is local to the 
    \env{definition}-environment:

    \stexexamplefile{ex/monoid_5_vardef.en.tex}[gobble=2,firstline=5,lastline=23,classoffset=1,morekeywords={
    \\mathstruct,\\monoid,\\universe,\\op,\\unit,\\fun,\\eq,\\inset
    },classoffset=9,morekeywords={
      \\vx
    }]
  
  \end{sfragment}
\end{document}