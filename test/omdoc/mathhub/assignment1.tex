\documentclass[minimal]{omdoc}
\usepackage[mh]{hwexam}
\input{localpath}
\begin{document}
We first include from the current repos

\inputmhassignment{BAR} 
\includemhassignment{BAR} 

And then we give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\inputassignment[mhrepos=omdoc/mathhub]{BAR} 
\includemhassignment[mhrepos=omdoc/mathhub]{BAR} 


\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
