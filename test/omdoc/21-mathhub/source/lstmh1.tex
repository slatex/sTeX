\documentclass[minimal]{omdoc}
\usepackage{lstmh}
\lstset{language=[LaTeX]TeX}
\begin{document}

We first include an image from the current repos:

first inline: \lstinputmhlisting{FOO.tex} and then centered

\clstinputmhlisting[linewidth=10cm]{FOO.tex}


And then we give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\lstinputmhlisting[mhrepos=omdoc/21-mathhub]{FOO.tex} 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
