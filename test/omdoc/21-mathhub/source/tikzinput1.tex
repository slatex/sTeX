\documentclass[minimal]{omdoc}
\usepackage[mh]{tikzinput}
\begin{document}

We first include an image from the current repos:

first inline: \tikzinput{bbt7} and then centered

\ctikzinput{bbt7}


And then we give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\tikzinput[mhrepos=omdoc/otherarchive]{bbt7} 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
