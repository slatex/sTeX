\documentclass[minimal]{omdoc}
\usepackage[mh]{modules}
\input{localpath}
\begin{document}
We first include from the current repos

\mhinputref{BAR} 

And then we give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\mhinputref[omdoc/mathhub]{BAR} 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
