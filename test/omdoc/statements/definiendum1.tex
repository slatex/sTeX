\documentclass[minimal]{omdoc}
%\usepackage[defindex]{statements}
\usepackage[defindex]{statements}
\begin{document}
\begin{module}[id=foomod]
  \begin{definition}[id=test.one]
    This \definiendum[name=foo,recaps=FOO?BAR]{foobar} is very explicit. We can reference
    it via \termref{foo}, \termref[name=foo]{foobar}, or even
    \termref[name=foo,cd=foomod]{foobar}; they should all come out the same.
  \end{definition}

  \begin{definition}[id=test.two]
    This \adefi[recaps=FOO?BAR]{foobar}{foo} should give the same result as the
    definiendum above. Again, we can reference it via \termref{foo},
    \termref[name=foo]{foobar}, or even \termref[name=foo,cd=foomod]{foobar}; they should
    all come out the same.
  \end{definition}

 \begin{definition}
   We also test definienda with math in them, e.g. a
   \defi[name=deriv]{$\mathcal C$-derivation}.

   And now for completeness the 2-4-ary variants: \defii[name=foo]{$\mathcal C$ foo}{bar}
   and \defiii[name=foo]{$\mathcal C$ foo}{bar}{baz} and \defiv[name=foo]{$\mathcal C$
     foo}{bar}{baz}{foobar}
 \end{definition}
\end{module}
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
