\documentclass[minimal]{omdoc}
\usepackage{smglom}
\begin{document}
We first import from the current repos
\gimport{FOO} 

And then we also give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\gimport[omdoc/smglom]{BAR} 
\gimport[omdoc/otherarchive]{BAZ} 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
