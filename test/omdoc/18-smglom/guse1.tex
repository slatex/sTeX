\documentclass[minimal]{omdoc}
\usepackage{smglom}
\begin{document}

\guse{BAR} 

And then we also give an explicit repos (this is the same as  the current one, but {\LaTeX}
doesn't know)

\guse[omdoc/smglom]{BAR} 
\end{document}
%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
